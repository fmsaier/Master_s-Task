using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ResetPYP : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        WholePYP.PYPElement = 4;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
